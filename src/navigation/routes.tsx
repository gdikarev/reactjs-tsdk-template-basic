interface Routes {
  home: string;
  registration: string;
  login: string;
  matches: string;
  profile: string;
  settings: string;
}

export const routes: Routes = {
  home: '/',
  registration: '/registration',
  login: '/login',
  matches: '/matches',
  profile: '/profile',
  settings: '/settings',
};